# sync by notion-stite
# use the setting below to set multiple docs directories.
# docSections = ["docs", "tutorials"]

uniqueHomePage = true # change to false to add sidebar to homepage

blogDir = "blog" # can be posts, blog e.t.c

repo = "https://github.com/pkwenda/notion-site"

time_format_blog = "Monday, January 02, 2006"
time_format_default = "January 2, 2006"
enableDarkMode = true # set to false to disable darkmode by default # user will still have the option to use dark mode

# sets the maximum number of lines per codeblock. The codeblock will however be scrollable and expandable.
codeMaxLines = 7

# disable showing line numbers by default. Switch to `true` if you'd rather have them on.
codeLineNumbers = false

description = "天南星科植物协会小组中文站"

# By default the template will look for icons under the icons directory. In some situations you might wanna change that. edit the line below
# iconsPath = 'icons/'

# 自定义样式或脚本
# customCSS = [styleURL1, styleURL2 ...]
# customJS = [scriptURL1, scriptURL2 ... ]
customJS = [https://raw.githubusercontent.com/artxia/CDN/scripts/aro_analytics.js]
# googleAnalytics = "G-4FWC681F7D"

# Site logo
[logo]
  lightMode = "images/logo.png"
  darkMode = "images/logo-dark.png"

#[source]
#  name = "GitHub"
#  iconLight = "images/GitHubMarkLight.svg"
#  iconDark = "images/GitHubMarkDark.svg"
#  url = "https://aroid.net"

# optional
# attribution. Feel free to delete this
[author]
  name = "aroid.net"
  url = "https://aroid.net"